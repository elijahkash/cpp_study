#include "shape.hpp"

#include <stdexcept>

/*template<int vertexCount>
template<typename Iterator>
ShapeLinearStorage<vertexCount>::ShapeLinearStorage(Iterator begin,
                                                    Iterator end) {
  for(uint32_t i = 0; i < vertexCount; i++) {
    if (begin == end)
      throw std::logic_error("Wrong initializer sequence.");
    vertices[i] = *begin;
    begin++;
  }
  if (begin != end)
    throw std::logic_error("Wrong initializer sequence.");
}*/
